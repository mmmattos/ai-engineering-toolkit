# Python Pandas

> Analyze and optimize pandas code using readable vectorized operations.

# Purpose

Analyze and optimize pandas code using readable vectorized operations. Review dtypes, missing values, joins, grouping, indexing, copies versus views, memory use, Python loops, null semantics, and duplicate keys. Optimize only where the trade-off is justified.
