# Embeddings

> Treat embeddings as a versioned data pipeline.

# Purpose

Treat embeddings as a versioned data pipeline. Review normalization, chunking, model and dimensions, batching, rate limits, retries, metadata, persistence, index compatibility, model changes, re-embedding, and retrieval evaluation. Separate ingestion, embedding generation, storage, and retrieval.
